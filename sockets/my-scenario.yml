config:
  target: "http://localhost:3000"
  phases:
    - duration: 20
      arrivalRate: 10
  socketio:
    transports: ["websocket"]
  processor: 'builder.js' 

scenarios:
  - name: My sample scenario
    engine: socketio
    flow:
      - function: "setMessage"

