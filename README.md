# WebSocket

Analyzing the performance of WebSockets

- Install dependencies:

```sh
   `npm install`
```

- Start server:

```sh
  `npm run dev`
```

- Run tests:

```sh
  `npm run performance`
```

